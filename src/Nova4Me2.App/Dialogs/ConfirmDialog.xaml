<Window x:Class="Nova4Me2.App.Dialogs.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirm" Width="520" SizeToContent="Height" WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource Bg}" Foreground="{DynamicResource Text}" FontFamily="{StaticResource UiFont}" FontSize="13" ShowInTaskbar="False">
  <StackPanel Margin="24">
    <TextBlock x:Name="TitleText" Style="{StaticResource H1}" FontSize="18" />
    <TextBlock x:Name="MessageText" Style="{StaticResource Body}" Margin="0,8,0,16" />
    <TextBlock x:Name="Prompt" Style="{StaticResource Muted}" Margin="0,0,0,6" />
    <TextBox x:Name="Input" Margin="0,0,0,18" />
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Content="Cancel" Click="Cancel_Click" IsCancel="True" />
      <Button x:Name="OkButton" Content="Proceed" Style="{StaticResource DangerButton}" Click="Ok_Click" IsEnabled="False" Margin="0" />
    </StackPanel>
  </StackPanel>
</Window>
